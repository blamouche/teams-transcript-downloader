# Todo — V3 : panneau latéral + overlay onglet dédié

Date : 2026-06-18
Branche : feature/v3-side-panel

## Objectif
V3 = V2 + :
- UI dans un **panneau latéral** (chrome.sidePanel) à droite, comme l'extension
  Claude (clic sur l'icône → ouvre le side panel). Plus de popup.
- **Overlay gris semi-transparent EN PERMANENCE** sur l'onglet Teams dédié, pour
  voir ce qui se passe tout en empêchant l'utilisateur de cliquer par erreur.

## Décisions utilisateur
- Overlay : en permanence sur l'onglet dédié (même automatisation OFF).
- UI : panneau latéral uniquement (popup supprimé, UI portée dans le panneau).

## Plan
- [x] Branche feature/v3-side-panel + copie v2 → v3.
- [x] Renommer popup.{html,css,js} → panel.{html,css,js}.
- [x] manifest v3 : version 3.0.0, permission `sidePanel`, `side_panel.default_path`,
      `action` sans default_popup, garder content_scripts.
- [x] panel.html : refs panel.css / panel.js. panel.css : largeur fluide (100%).
- [x] background.js :
      - `chrome.sidePanel.setPanelBehavior({ openPanelOnActionClick:true })`.
      - Overlay : injection via chrome.scripting dans dedicatedTabId (idempotent),
        bloque clic/clavier/scroll utilisateur, bandeau d'info. Re-injection au
        rechargement de l'onglet (onUpdated complete). Persiste en SPA (MutationObserver).
      - showOverlay appelé depuis ensureTeamsTab + onUpdated + au réveil du SW.
- [x] node --check, manifest JSON valide.
- [x] README mis à jour (3 versions, structure, installation).

## Review
- Dossier `v3/` : copie de v2 + `panel.{html,css,js}` (ex-popup), manifest 3.0.0
  avec `sidePanel`/`side_panel.default_path`, action sans popup.
- `background.js` : section voile (`pageApplyOverlay`/`pageRemoveOverlay` injectés,
  `showOverlay`/`hideOverlay`/`showOverlayOnDedicated`), `setPanelBehavior` au démarrage,
  `tabs.onUpdated` pour réinjecter après rechargement, `showOverlay` dans `ensureTeamsTab`.
- Validé : `node --check` (background/panel/content), manifest JSON OK.
- Limites / à tester en réel :
  - Vérifier que le voile ne gêne pas le rendu du récap (clics programmatiques OK en
    théorie). `hideOverlay` existe mais n'est jamais appelé (voile permanent voulu) —
    seule la fermeture de l'onglet le retire ; prévoir éventuellement un bouton
    « libérer l'onglet » plus tard.
  - `chrome.sidePanel` nécessite Chrome ≥ 114.
- Statut : completed.
